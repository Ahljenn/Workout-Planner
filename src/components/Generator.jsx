import React, { useState } from 'react'

function builder(name, count){
    
}

export default function Generator(props) {
    return (
        <div>
        {props.workouts}
        {props.count}
        </div>
    )
}
